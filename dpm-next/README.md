# Dynamic Pictures Media Ltd — web app

## Scripts

- `npm run dev` — local development
- `npm run build` — production build
- `npm run start` — run production server

## Assets

Place media in `public/images/` by category (`brand`, `hero`, `services`, `gallery`, `team`, `testimonials`, `events`). Update paths in `src/lib/data.ts` when adding new files.
